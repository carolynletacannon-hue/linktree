import { ArrowUpRight } from 'lucide-react'; import { streamingLinks } from '../data/links'; import { BrandIcon } from './Icons';
export function StreamingLinks(){return <section id="listen" className="section stream"><div className="stream-intro reveal"><p className="eyebrow">Every note. Your way.</p><h2>Listen<br/><em>Everywhere</em></h2><p>Stream Carolyn Cannon wherever you listen. Follow along to be first to hear new releases.</p></div><div className="platform-list reveal">{streamingLinks.map((x,i)=><a className={i===0?'featured':''} href={x.url} target="_blank" rel="noreferrer" key={x.name} aria-label={`Listen to Carolyn Cannon on ${x.name}`}><BrandIcon name={x.name}/><span>{x.name}</span>{i===0&&<small>Preferred</small>}<ArrowUpRight/></a>)}</div></section>}
