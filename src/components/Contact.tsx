import { ArrowUpRight,Instagram,Facebook,Youtube,Music2 } from 'lucide-react'; import {siteConfig,socialLinks} from '../data/links';
const icons=[Instagram,Music2,Youtube,Music2,Facebook,Music2];
export function Contact(){return <section id="contact" className="contact section"><div className="reveal"><p className="eyebrow">Professional inquiries</p><h2>Let’s make<br/>something <em>real.</em></h2></div><div className="contact-side reveal"><p>Management, collaboration, production, and business inquiries</p><a className="email" href={`mailto:${siteConfig.email}`}>{siteConfig.email}<ArrowUpRight/></a><div className="socials">{socialLinks.map((x,i)=>{const Icon=icons[i];return <a href={x.url} target="_blank" rel="noreferrer" aria-label={x.name} key={x.name}><Icon/></a>})}</div></div></section>}
