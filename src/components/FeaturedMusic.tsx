import { ExternalLink } from 'lucide-react'; import { releases } from '../data/releases'; import { AudioPlayer } from './AudioPlayer';
export function FeaturedMusic(){return <section id="music" className="section music"><header className="section-head reveal"><div><p className="eyebrow">Selected releases</p><h2>Featured Music</h2></div><p>Listen to the latest songs and find Carolyn on your favorite platform.</p></header><div className="release-grid">{releases.map((r,i)=><article className="release-card reveal" key={r.id}><div className="art"><img src={r.artwork} alt={`${r.title} cover artwork`}/><span>0{i+1}</span></div><div className="release-info"><p>{r.meta}</p><h3>{r.title}</h3><span>{r.artist}</span><AudioPlayer src={r.audio} title={r.title}/><a className="text-link" href={Object.values(r.links)[0]||'#listen'} target={Object.values(r.links)[0]?'_blank':undefined} rel="noreferrer">Listen everywhere <ExternalLink size={14}/></a></div></article>)}</div></section>}
