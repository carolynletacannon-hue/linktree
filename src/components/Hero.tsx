import { ArrowDown } from 'lucide-react'; import { streamingLinks,siteConfig } from '../data/links'; import { BrandIcon } from './Icons';
export function Hero(){return <section id="top" className="hero"><div className="hero-photo" style={{backgroundImage:`linear-gradient(90deg,rgba(16,15,15,.06),rgba(16,15,15,.48)),url(${siteConfig.heroImage})`}} role="img" aria-label="Carolyn Cannon wearing a red sequined top"/><div className="hero-copy reveal"><p className="eyebrow">Independent recording artist</p><h1>Carolyn<br/><em>Cannon</em></h1><p className="role">Artist <i/> Producer <i/> Songwriter</p><p className="tagline">Music built from emotion,<br/>story, and sound.</p><a className="button primary" href="#listen">Listen now <ArrowDown size={17}/></a><div className="hero-platforms" aria-label="Streaming platforms">{streamingLinks.slice(0,5).map(x=><a href={x.url} target="_blank" rel="noreferrer" aria-label={`Carolyn Cannon on ${x.name}`} key={x.name}><BrandIcon name={x.name}/></a>)}</div></div><div className="scroll-note">Scroll to discover <span/></div></section>}
