import { useEffect,useState } from 'react'; import { Menu,X } from 'lucide-react';
export function Navbar(){const [open,setOpen]=useState(false);const [scrolled,setScrolled]=useState(false);useEffect(()=>{const f=()=>setScrolled(scrollY>24);addEventListener('scroll',f,{passive:true});return()=>removeEventListener('scroll',f)},[]);const nav=[['About','#about'],['Listen','#listen'],['Contact','#contact']];return <header className={`navbar ${scrolled?'scrolled':''}`}><a className="wordmark" href="#top">Carolyn Cannon</a><button className="menu" onClick={()=>setOpen(!open)} aria-expanded={open} aria-controls="navlinks" aria-label="Toggle navigation">{open?<X/>:<Menu/>}</button><nav id="navlinks" className={open?'open':''} aria-label="Main navigation">{nav.map(([n,h])=><a key={n} href={h} onClick={()=>setOpen(false)}>{n}</a>)}</nav></header>}
